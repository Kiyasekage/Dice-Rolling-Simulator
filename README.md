# Dice-Rolling-Simulator
A Python program that simulates rolling two six-sided dice using Python's built-in random module. The program allows users to roll the dice repeatedly until they choose to stop, demonstrating loops, user interaction, and random number generation.
